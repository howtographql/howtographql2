import * as React from 'react';

import Logo from './Logo';

// interface Props {
//   steps: { [key: string]: Step[] };
//   siteTitle: any;
// }

export default ({ siteTitle }) => {
  return (
    <div>
      <Logo />
    </div>
  );
};
